# views/pagination-view.coffee

window.app = window.app || {}

window.app.PaginationView = Backbone.View.extend

    tagName: 'div'

    className: 'pagination-container'

    template: _.template $('#pagination-template').html()

    initialize: ->
        #
        return
    
    render: ->
        #
        return this

    
